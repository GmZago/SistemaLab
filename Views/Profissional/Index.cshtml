@model IEnumerable<SistemaExame.Models.Profissional>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProfissionalNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cargo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProfissionalNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cargo.CargoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfissionalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProfissionalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfissionalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
